package com.example.flappybird;

/**
 * This class is used to store variables which will be needed in most functions and methods. It
 * makes it easier to use "Constants.SCREEN_WIDTH || Constants.SCREEN_HEIGHT" than redefine it
 * for every usage.
 *
 * @class Constants
 */

public class Constants {

    // Creates the variables which store screen dimensions. Defined in "PlayActivity".
    public static int SCREEN_WIDTH;
    public static int SCREEN_HEIGHT;
}
